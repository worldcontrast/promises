name: World Contrast — Scout Discovery Agent

on:
  workflow_dispatch:
    inputs:
      country:
        description: 'Sigla do País (Ex: BR, FR, US)'
        required: true
        default: 'BR'
      election:
        description: 'Nome da Eleição (Ex: Presidential 2026)'
        required: true
        default: 'Presidential 2026'

permissions:
  contents: write

jobs:
  run-scout:
    name: OSINT Scout Runner
    runs-on: ubuntu-latest
    timeout-minutes: 15 # Proteção: Impede que o script rode infinitamente se a API travar

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Garante o histórico completo para evitar conflitos no push

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip' # Acelera a execução fazendo cache das bibliotecas

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install httpx anthropic tavily-python duckduckgo-search

      - name: Run Scout Agent
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          TAVILY_API_KEY: ${{ secrets.TAVILY_API_KEY }}
        run: |
          python agents/scout.py \
            --country "${{ github.event.inputs.country }}" \
            --election "${{ github.event.inputs.election }}" \
            --search tavily

      - name: Commit and Push JSON (O Carimbo Humano)
        run: |
          git config --global user.name "World Contrast Scout"
          git config --global user.email "scout@worldcontrast.org"
          git add data/countries/
          
          # Lógica condicional elegante: só tenta commitar se houver mudanças reais
          if git diff-index --quiet HEAD --; then
            echo "✅ Nenhuma mudança ou candidato novo encontrado na varredura."
          else
            git commit -m "🤖 Scout: Base de candidatos atualizada para ${{ github.event.inputs.country }} - ${{ github.event.inputs.election }}"
            
            # Puxa as últimas atualizações antes de enviar, evitando erros de colisão no GitHub
            git pull --rebase origin main
            git push
          fi
